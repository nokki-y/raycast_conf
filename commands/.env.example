# ==================================================
# スクリプトコマンド用 環境変数テンプレート
# ==================================================
# このファイルを .env にコピーして使用してください:
#   cp .env.example .env
#
# ⚠️ 注意: .env ファイルは .gitignore に含まれており、
#          Gitで追跡されません（秘匿情報保護のため）
# ==================================================

# --------------------------------------------------
# MetaLife 設定
# --------------------------------------------------
# MetaLife スペースID
# 実際の値を .env ファイルに設定してください
METALIFE_SPACE_ID=your_space_id_here

# --------------------------------------------------
# Google アカウント設定
# --------------------------------------------------
# Googleアカウントインデックス（複数アカウントを使用している場合）
# デフォルト: 0（最初のアカウント）
# 2番目のアカウントを使用する場合は 1 を設定
GOOGLE_ACCOUNT_INDEX=0

# --------------------------------------------------
# GitHub 設定
# --------------------------------------------------
# GitHubリポジトリ（カンマ区切りで複数指定可能）
# 例: GITHUB_REPOSITORIES=org/repo1,org/repo2,user/repo3
#
# ⚠️ 重要: 環境変数を変更した場合は、以下のコマンドでビルドを実行してください:
#   ./scripts/build-commands.sh
#
# これにより、Raycastの「Open GitHub」コマンドのドロップダウンに
# リポジトリが表示されるようになります。
GITHUB_REPOSITORIES=your_repositories_here

# ==================================================
# セキュリティ警告
# ==================================================
# ⚠️ 本物の認証情報・APIキーをこのファイルに記載しないでください
# ⚠️ 実際の値は .env ファイルにのみ記載してください
# ⚠️ .env ファイルは絶対にGitにコミットしないでください
# ==================================================
